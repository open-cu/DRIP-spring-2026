spring:
  application:
    name: delayed-delivery-app
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    listener:
      simple:
        auto-startup: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
