spring:
  application:
    name: rabbitmq-demo
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    listener:
      simple:
        auto-startup: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
